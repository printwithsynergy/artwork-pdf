{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm turbo build --filter=@artworkpdf/service..."
  },
  "deploy": {
    "startCommand": "pnpm --filter @artworkpdf/service start",
    "healthcheckPath": "/healthz"
  }
}
