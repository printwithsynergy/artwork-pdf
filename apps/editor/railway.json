{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd ../.. && pnpm install --frozen-lockfile && pnpm turbo build --filter=@artworkpdf/editor..."
  },
  "deploy": {
    "startCommand": "cd ../.. && pnpm --filter @artworkpdf/editor start",
    "healthcheckPath": "/"
  }
}
